---
import BaseLayout from "../layouts/BaseLayout.astro";
import ProjectCard from "../components/ProjectCard.astro";
import { projects } from "../data/projects";

const bySlug = (slug: string) => projects.find((p) => p.slug === slug);

// Grouped by HOW I work, not just when, tells the positioning story.
const groups = [
  {
    eyebrow: "Industrial SaaS · Design systems",
    title: "Where I go deep.",
    blurb:
      "Inside NETZSCH's product lines: the on-machine HMIs, the B2B portals, and the design systems that hold them together.",
    slugs: [
      "iris-v3",
      "netzsch-design-system",
      "norius",
      "netzsch-customer-portal",
      "digital-reports",
      "iris-design-system",
    ],
  },
  {
    eyebrow: "AI-native · Solo builds",
    title: "Where I move fast.",
    blurb:
      "Products I design and build end to end, with AI as my build partner, shipped because I wanted them to exist.",
    slugs: ["nerida", "techcolab", "garmin-coach", "mosey"],
  },
  {
    eyebrow: "Foundations",
    title: "Where I came from.",
    blurb:
      "Public sector, fintech, and enterprise work, plus the indie games I started with.",
    slugs: [
      "myct-connecticut",
      "superdigital",
      "dell-expert-network",
      "eye",
      "gan-dath",
    ],
  },
].map((g) => ({ ...g, items: g.slugs.map(bySlug).filter(Boolean) }));

const yearSpan = `${Math.min(...projects.map((p) => parseInt(p.year)))} → ${Math.max(...projects.map((p) => parseInt(p.year)))}`;
---

<BaseLayout
  title="Work · Gabriel Oliveira"
  description="Case studies across industrial SaaS, design systems, AI-native tools, fintech, and public-sector platforms."
>
  <!-- Page hero -->
  <section class="max-w-[var(--container-wide)] mx-auto px-6 md:px-10 pt-14 md:pt-20 pb-12 md:pb-16">
    <span class="font-mono text-[11px] uppercase tracking-wider text-[var(--color-fg-subtle)]">
      Work · {projects.length} projects · {yearSpan}
    </span>
    <h1
      class="mt-3 text-4xl md:text-6xl font-extrabold display"
      style="letter-spacing:-0.045em;"
    >
      Everything I've shipped<br />since 2016.
    </h1>
    <p class="mt-5 text-lg text-[var(--color-fg-muted)] max-w-2xl leading-relaxed">
      Every project across industrial SaaS, design systems, and AI-native
      tools, grouped by how I work, not just by when.
    </p>
  </section>

  <!-- Grouped work -->
  {groups.map((group, gi) => (
    <section class={`max-w-[var(--container-wide)] mx-auto px-6 md:px-10 py-12 md:py-16 ${gi === 0 ? "border-t" : "border-t"} border-[var(--color-border)]`}>
      <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-4 mb-8 md:mb-10" data-reveal>
        <div>
          <span class="font-mono text-[10px] uppercase tracking-wider text-[var(--color-accent)]">
            {group.eyebrow}
          </span>
          <h2 class="mt-2 text-2xl md:text-4xl font-extrabold display" style="letter-spacing:-0.035em;">
            {group.title}
          </h2>
        </div>
        <p class="text-sm md:text-[15px] text-[var(--color-fg-muted)] leading-relaxed max-w-sm md:text-right">
          {group.blurb}
        </p>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-5">
        {group.items.map((project, i) => (
          <ProjectCard project={project} index={i} size="normal" />
        ))}
      </div>
    </section>
  ))}

  <!-- CTA -->
  <section class="max-w-[var(--container-wide)] mx-auto px-6 md:px-10 py-20 border-t border-[var(--color-border)]">
    <div class="flex flex-col gap-5 max-w-2xl">
      <span class="font-mono text-[10px] uppercase tracking-wider text-[var(--color-fg-subtle)]">
        Working on something hard?
      </span>
      <h2 class="text-3xl md:text-5xl font-extrabold display" style="letter-spacing:-0.045em;">
        Let's get into the weeds together.
      </h2>
      <a
        href="mailto:gabriel@oliveibriel.com"
        class="self-start inline-flex items-center gap-2 px-5 py-3 rounded-md bg-[var(--color-fg)] hover:bg-[var(--color-accent)] text-white text-sm font-medium transition-colors mt-2"
      >
        gabriel@oliveibriel.com
        <svg class="size-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M7 17 17 7M7 7h10v10"/>
        </svg>
      </a>
    </div>
  </section>
</BaseLayout>
